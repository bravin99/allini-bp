@inherits LayoutComponentBase

<MudThemeProvider @bind-IsDarkMode="@_isDarkMode" Theme="_theme"/>
<MudDialogProvider/>
<MudSnackbarProvider/>

<MudLayout>
    <MudAppBar Elevation="1">
        <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start" OnClick="@((e) => DrawerToggle())" />
        <MudButton Variant="Variant.Text" Typo="Typo.h5" Class="ml-3" DisableElevation="true" Color="Color.Inherit" Href="/">ALLINI BUSINESS PLATFORM</MudButton>
        <MudSpacer />
        <LoginDisplay />
        <MudToggleIconButton @bind-Toggled="@_isDarkMode" Icon="@Icons.Material.Filled.DarkMode" Title="Enable dark mode" Color="Color.Inherit"
                     ToggledIcon="@Icons.Material.Filled.LightMode" ToggledTitle="Disable Dark Mode" Class="ms-2" />
    </MudAppBar>
    <MudDrawer @bind-Open="_drawerOpen" ClipMode="DrawerClipMode.Always" Elevation="2">
        <NavMenu />
    </MudDrawer>
    <MudMainContent>
        <MudContainer class="mt-3">
            @Body
        </MudContainer>
    </MudMainContent>
</MudLayout>

@code {
    private MudTheme _theme = new();
    bool _drawerOpen = true;
    bool _isDarkMode = true;

    void DrawerToggle()
    {
        _drawerOpen = !_drawerOpen;
    }
}

@* <MudThemeProvider @bind-IsDarkMode="@_isDarkMode" Theme="_theme"/>
<MudDialogProvider/>
<MudSnackbarProvider/>

<MudLayout>
    <MudAppBar Color="Color.Inherit" Fixed="false">
        <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start" OnClick="@((e) => DrawerToggle())" />
        <MudButton Variant="Variant.Text" DisableElevation="true" Color="Color.Inherit" Href="/">ALLINI BUSINESS PLATFORM</MudButton>
        <MudSpacer />
        
        <LoginDisplay />
        <MudToggleIconButton @bind-Toggled="@_isDarkMode" Icon="@Icons.Material.Filled.DarkMode" Title="Enable dark mode" Color="Color.Inherit"
                     ToggledIcon="@Icons.Material.Filled.LightMode" ToggledTitle="Disable Dark Mode" Class="ms-2" />
    </MudAppBar>
    <MudDrawer @bind-Open="@_drawerOpen" ClipMode="DrawerClipMode.Always" Variant="DrawerVariant.Persistent" Elevation="1">
        <NavMenu/>
    </MudDrawer>
    <MudMainContent Class="ms-3 me-3 mb-3">
        @Body
    </MudMainContent>
</MudLayout>

@code {
    private MudTheme _theme = new();
    private bool _isDarkMode = true;
    bool _drawerOpen = true;

    void DrawerToggle()
    {
        _drawerOpen = !_drawerOpen;
    }

} *@